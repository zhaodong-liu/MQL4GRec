name: MQL_train
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10
  - pytorch=2.1.0
  - pytorch-cuda=12.1
  - torchvision
  - torchaudio
  - pip
  - pip:
      - transformers<=4.45.0
      - accelerate==0.28.0
      - datasets
      - sentencepiece
      - protobuf